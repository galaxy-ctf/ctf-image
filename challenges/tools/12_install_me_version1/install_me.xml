<tool id="install_me" name="Install me" version="1.1">
  <command detect_errors="aggressive"><![CDATA[
      #if $input and $hint:
        echo "Don't be greedy, either ask for a hint or the flag, not both! ;)" > $output
      #elif $input:
        echo "Well done admin! However, the flag is no longer here!" > $output
      #elif $hint:
        echo "Remember the back_in_time challenge?" > $output
      #else:
        echo "You didn't ask for a hint or a flag" > $output
      #end if
  ]]></command>
  <inputs>
      <param name="input" type="boolean" checked="false" label="Would you like a flag?"/>
      <param name="hint" type="boolean" checked="false" label="Would you like a hint?" />
  </inputs>
  <outputs>
    <data format="txt" name="output" />
  </outputs>
  <help><![CDATA[
You did it! You installed me! Now run me to get the flag
  ]]></help>
  <citations/>
</tool>
